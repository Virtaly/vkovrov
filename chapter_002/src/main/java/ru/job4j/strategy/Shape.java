package ru.job4j.strategy;

/**
 * Интерфейс геометрической фигуры.
 */
public interface Shape {
    /**
     * Метод для создания строки, при выводе которой на экран должна получиться геометрическая фигура.
     * @return строка, при выводе которой на экран должна получиться геометрическая фигура.
     */
    String pic();
}
